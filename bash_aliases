#!/bin/bash

# ls shortcuts
alias ll='ls --color -alF'
alias la='ls --color -A'
alias l='ls --color -CF'

# cd's
alias ..='cd ..'
alias ...='cd ../..'


# navigation shortcuts
alias home='cd ~/'
alias dot='cd ~/dotfiles'

# neovim
alias nv='nvim'
